/**
 * Injective EVM network definitions for the x402 protocol.
 *
 * Chain IDs:
 *   Mainnet: 1776  (CAIP-2: eip155:1776)
 *   Testnet: 1439  (CAIP-2: eip155:1439)
 */
import { type Chain } from "viem";
export declare const injectiveEvm: {
    readonly id: 1776;
    readonly name: "Injective EVM";
    readonly nativeCurrency: {
        readonly name: "Injective";
        readonly symbol: "INJ";
        readonly decimals: 18;
    };
    readonly rpcUrls: {
        readonly default: {
            readonly http: readonly ["https://sentry.evm-rpc.injective.network"];
        };
        readonly public: {
            readonly http: readonly ["https://sentry.evm-rpc.injective.network"];
        };
    };
    readonly blockExplorers: {
        readonly default: {
            readonly name: "Blockscout";
            readonly url: "https://blockscout.injective.network";
        };
    };
    readonly contracts: {};
};
export declare const injectiveEvmTestnet: {
    readonly id: 1439;
    readonly name: "Injective EVM Testnet";
    readonly nativeCurrency: {
        readonly name: "Injective";
        readonly symbol: "INJ";
        readonly decimals: 18;
    };
    readonly rpcUrls: {
        readonly default: {
            readonly http: readonly ["https://k8s.testnet.json-rpc.injective.network"];
        };
        readonly public: {
            readonly http: readonly ["https://k8s.testnet.json-rpc.injective.network"];
        };
    };
    readonly blockExplorers: {
        readonly default: {
            readonly name: "Blockscout Testnet";
            readonly url: "https://testnet.blockscout.injective.network";
        };
    };
    readonly testnet: true;
    readonly contracts: {};
};
export declare const INJECTIVE_MAINNET_CAIP2: "eip155:1776";
export declare const INJECTIVE_TESTNET_CAIP2: "eip155:1439";
export type InjectiveNetwork = typeof INJECTIVE_MAINNET_CAIP2 | typeof INJECTIVE_TESTNET_CAIP2;
export interface TokenConfig {
    address: `0x${string}`;
    decimals: number;
    symbol: string;
    name: string;
    /** Whether the token implements EIP-3009 transferWithAuthorization */
    eip3009: boolean;
    /** EIP-712 domain version string (default "2", used by Circle FiatTokenV2_2) */
    eip712Version?: string;
}
/**
 * Known token addresses on Injective EVM.
 *
 * Status of EIP-3009 (transferWithAuthorization) on native tokens:
 *   - USDC mainnet (0xa00C…235a): Circle FiatTokenV2_2 — full EIP-3009 + EIP-2612.
 *                                  Use this for x402 payments. Domain version "2".
 *   - USDC testnet (0x0C38…4C5d): Circle FiatTokenV2_2 — same shape as mainnet.
 *   - USDT (0x88f7…Cc13): plain IBC-bridged ERC-20 — NO EIP-3009.
 *   - WINJ (0x0000…FfB):  plain ERC-20 — NO EIP-3009.
 *
 * Non-EIP-3009 tokens such as USDT and WINJ cannot be used directly with x402;
 * they are listed for informational purposes only.
 *
 * The legacy IBC-bridged USDC at 0x2a25…3989 is intentionally not registered
 * here. It has no EIP-3009 and is superseded by the native Circle deployment.
 *
 * For any custom EIP-3009 token your integration needs, register it at startup
 * via {@link registerToken} rather than setting environment variables — that
 * keeps this module pure and safe to bundle for browsers.
 */
export declare const TOKENS: Record<InjectiveNetwork, Record<string, TokenConfig>>;
/**
 * Register (or override) a token entry at runtime.
 *
 * Useful when your integration uses a custom EIP-3009 token that is not in the
 * canonical registry yet. Keeps this module pure — no `process.env` reads at
 * import time, which means it is safe to bundle for browsers or Deno/Bun
 * runtimes.
 *
 * @example
 *   registerToken("eip155:1776", "MYTOKEN", {
 *     address:       "0x…",
 *     decimals:      6,
 *     symbol:        "MYTOKEN",
 *     name:          "My EIP-3009 Token",
 *     eip3009:       true,
 *     eip712Version: "2",
 *   });
 */
export declare function registerToken(network: InjectiveNetwork, symbol: string, config: TokenConfig): void;
export declare function isInjectiveNetwork(network: string): network is InjectiveNetwork;
export declare function getViemChain(network: InjectiveNetwork): Chain;
export declare function getRpcUrl(network: InjectiveNetwork): string;
export declare function getToken(network: InjectiveNetwork, symbol: string): TokenConfig | undefined;
export declare function getTokenByAddress(network: InjectiveNetwork, address: string): TokenConfig | undefined;
//# sourceMappingURL=index.d.ts.map